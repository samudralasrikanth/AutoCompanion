{
  "name": "@automation-studio/recorder",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@automation-studio/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "playwright": "^1.62.1",
    "typescript": "~5.6.0",
    "vitest": "^2.0.0"
  }
}
