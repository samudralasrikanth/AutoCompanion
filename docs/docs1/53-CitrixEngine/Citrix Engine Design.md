# Vision Engine Design

Pipeline Capture → OCR → Template Match → Anchor Resolution → Confidence
Score → Execute

Fallback Order UIA (if available) Image OCR Anchor Coordinates

Performance Goal \<500ms average element resolution.
