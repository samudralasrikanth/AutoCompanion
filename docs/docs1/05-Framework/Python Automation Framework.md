# Python Framework

Package layout

as_runner/ as_framework/ as_reporting/

Core decorators

@step() @before_suite @after_suite @before_step @after_step

Utilities attach_screenshot() get_credential() log()
