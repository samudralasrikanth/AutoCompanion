import type { ICommandDescriptor, IServiceProvider } from '@automation-studio/types';

export function createRecorderCommands(_provider: IServiceProvider): ReadonlyArray<ICommandDescriptor> {
  return [];
}

